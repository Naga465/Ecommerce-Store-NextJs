/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  swcMinify: true,
  env: {
    author : 'Nagasai',
    github : 'htpps://github.com/naga465'
  },
  
}

module.exports = nextConfig
